# Where To Go Django

Это мини‑проект на Django, который показывает интерактивную карту с местами (точками) и картинками для этих мест.  
Карты отрисовываются с помощью **Leaflet**, справа подключается **Leaflet Sidebar**.  
Изображения хранятся в `/media/places`, и загружаются через Django Admin.

Для удобного наполнения базы данных есть команда управления, которая запрашивает место из внешнего API и
сохраняет его в базу. Для тестого сценария есть также локальный эндпоинт, который возвращает случайное место.
./manage.py load_place ['http://127.0.0.1:8000/maps/random-place/' |
'https://taisya451.pythonanywhere.com/maps/random-place/' | 'HOSTING_API_URL/maps/random-place/']
---

## 1. Структура проекта (полная)

```
.
├── db.sqlite3                     ← SQLite база данных
├── manage.py                      ← точка входа в Django
├── maps                           ← Django приложение
│   ├── admin.py                   ← регистрация моделей в Django Admin
│   ├── apps.py                    ← config приложения maps
│   ├── models.py                  ← модели Place и PlaceImage
│   ├── urls.py                    ← роуты приложения maps
│   ├── views.py                   ← отображение карты / index
│   ├── migrations                 ← Django миграции
│   ├── static/maps                ← статика ЛОКАЛЬНОГО приложения maps
│   │   ├── favicon.png
│   │   ├── hand-pointer-regular.svg
│   │   ├── leaflet-sidebar.css
│   │   ├── leaflet-sidebar.js
│   │   └── places                 ← статические картинки (если нужны)
│   └── templates/maps/index.html  ← HTML страница с картой
├── media/places                   ← ЗАГРУЖЕННЫЕ пользователем изображения МЕСТ
└── where_to_go_django             ← главный Django проект
    ├── settings.py                ← настройки Django
    ├── urls.py                    ← корневой URL роутер
    ├── wsgi.py / asgi.py          ← запуск Django server (WSGI/ASGI)
```

---

## 2. Что делает проект

1) На странице `http://127.0.0.1:8000/maps/` открывается карта Leaflet
2) При клике по элементу (месту) — показываются фотографии места, которые хранятся в `/media/places` и его описание

Вся база данных о местах заполняется в **Django Admin**.

---

## 3. Требования

### Python пакеты

- Python 3.8+
- Django 5.2.7
- django-ckeditor 6.7.3 (WYSIWYG редактор)
- Pillow 12.0.0 (обработка изображений)
- python-dotenv 1.2.1 (переменные окружения)
- spatialite 0.0.3 (геопространственная БД)
- asgiref 3.10.0 (ASGI совместимость)
- sqlparse 0.5.3 (парсинг SQL)

### Системные зависимости

- spatialite-bin (Linux: `sudo apt-get install spatialite-bin libsqlite3-mod-spatialite`)
- libsqlite3-mod-spatialite

### Базы данных

- SQLite3 с Spatialite (разработка)
- PostgreSQL с PostGIS (продакшн)

### Фронтенд

- Leaflet.js (карты)
- JavaScript (ES6+)
- Современный браузер с поддержкой GeoJSON

---

## 4. Установка

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install django pillow
```

---

## 5. Создание базы данных

```bash
python manage.py migrate
```

Создать админа:

```bash
python manage.py createsuperuser
```

---

## 6. Запуск сервера

```bash
python manage.py runserver
```

открыть в браузере:

http://127.0.0.1:8000/maps/

запросить данные о месте (JSON):
http://127.0.0.1:8000/maps/places/1/

---

## 7. MEDIA / STATIC — очень важно

В `where_to_go_django/settings.py` должно быть:

```python
MEDIA_URL = "/media/"
MEDIA_ROOT = BASE_DIR / "media"
STATIC_URL = "/static/"
```

И в `urls.py`:

```python
from django.conf import settings
from django.conf.urls.static import static

urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

Это нужно ТОЛЬКО в DEBUG режиме (локальная разработка).

---

## 8. Админка

тут загружаются места и изображения к ним:

```
http://127.0.0.1:8000/admin/
```

# Модели и Админ-панель

## 📊 Модель Place

### Поля модели:

| Поле                | Тип                   | Описание                                           |
|---------------------|-----------------------|----------------------------------------------------|
| `title`             | CharField(255)        | Название локации                                   |
| `description_short` | TextField             | Короткое описание (простой текст)                  |
| `description_long`  | TextField             | Длинное описание (поддерживает HTML через WYSIWYG) |
| `point`             | PointField(srid=4326) | Географическая точка в системе координат WGS84     |

### Свойства (properties):

| Свойство     | Возвращает | Описание                              |
|--------------|------------|---------------------------------------|
| `lat`        | float      | Широта точки (y-координата)           |
| `lng`        | float      | Долгота точки (x-координата)          |
| `image_urls` | list[str]  | Список URL всех связанных изображений |

### Методы:

| Метод       | Возвращает | Описание                             |
|-------------|------------|--------------------------------------|
| `to_dict()` | dict       | Словарь с данными места для JSON API |
| `__str__()` | str        | Название места для отображения       |

## 🖼 Модель PlaceImage

### Поля модели:

| Поле    | Тип               | Описание                                          |
|---------|-------------------|---------------------------------------------------|
| `place` | ForeignKey(Place) | Связь с родительским местом                       |
| `image` | ImageField        | Файл изображения (сохраняется в `/media/places/`) |

### Свойства:

| Свойство | Возвращает | Описание                     |
|----------|------------|------------------------------|
| `url`    | str        | URL загруженного изображения |

## 🛠 Админ-панель

### PlaceAdmin - управление местами

#### Возможности:

- **📝 WYSIWYG редактор** для поля `description_long` (CKEditor)
- **🗺️ Управление координатами** через отдельные поля широты/долготы
- **🖼️ Inline-изображения** - добавление фото прямо при создании места
- **🔍 Поиск** по названию места
- **📋 Список мест** с отображением ID, названия, короткого описания и координат

#### Форма PlaceAdminForm:

- `description_long` - поле с CKEditor для форматированного текста
- `lat`, `lng` - числовые поля для ввода координат
- **Автоконвертация** координат в Point объект при сохранении

### PlaceImageAdmin - управление изображениями

#### Возможности:

- **👀 Превью изображений** в списке и inline
- **🔗 Кликабельные превью** (открывают оригинал в новой вкладке)
- **🔍 Поиск** по названию связанного места
- **📋 Отображение** места и URL изображения

### PlaceImageInline - inline для изображений

#### Особенности:

- **➕ 3 пустых слота** для быстрого добавления изображений
- **🖼️ Превью в реальном времени**
- **📏 Ограничение высоты** превью (50px)
- **🎯 Интеграция** в форму редактирования места

## 🔧 Технические детали

### WYSIWYG редактор:

- **Библиотека**: django-ckeditor
- **Поле**: description_long
- **Форматирование**: HTML с поддержкой стилей, ссылок, списков

### Геоданные:

- **Формат**: PointField (PostGIS/Spatialite)
- **SRID**: 4326 (WGS84 - стандарт для GPS)
- **Преобразование**: автоматическое из lat/lng в Point

### Изображения:

- **Путь загрузки**: `media/places/`
- **Автоименование**: оригинальные имена файлов
- **Превью**: уменьшенные версии в админке

### API данные:

- **Формат**: JSON через метод `to_dict()`
- **Поля**: title, placeId, lat, lng, imgs[], description_short, description_long
- **HTML поддержка**: description_long содержит форматированный HTML

---

## 9. Когда что‑то не работает

| проблема                    | причина                         |
|-----------------------------|---------------------------------|
| картинки не показываются    | MEDIA не подключены в urls      |
| карта белая                 | статика Leaflet не подгрузилась |
| sidebar не открывается      | нет JS leaflet‑sidebar.js       |
| картинки в sidebar не видны | файл не лежит в `/media/places` |

---

## 10. Данные в SQLite

SQLite здесь — просто для примера.  
В продакшене — лучше PostgreSQL+PostGIS.

---

## 11. Лицензия

Зависит от вас. Можно MIT.

---

## 12. Автор

Aleksandr Navitskas

